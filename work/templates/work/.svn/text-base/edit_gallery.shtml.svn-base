{% extends "base.shtml"%}
{% load i18n %}{% load comments %}{% load thumbnail %}{% load taggit_extras %}
{% block title %}修改 {{chapter.title}} {% endblock %}

{% block extra_include %}
	{% include "style.html" %}
	{% include "style-form.html" %}
	{% include "style-project.html" %}
	{% include "forms.html" %}
{% endblock %}

<link rel="stylesheet" href="/style/jquery.confirm.css" />

{% block content %}
<body class="p-chapter-edit si">{% include "inc/navi.html" %}
	<!-- 新建图集页就不做了…… 都差不多 -->
	<div class="snav">
		<div class="project-info">
		{% include "inc/project/info.shtml" %}
		</div>
		<ul class="browse-path">
			<li><a href="{{work.get_absolute_url}}">{{work.name}}</a><ul>
			<li><a href="{{chapter.get_absolute_url}}">{{chapter.title}}</a><ul>
				<li><h1><a href=".">修改</a></h1></li>
			</ul></li>
			</ul></li>
		</ul>
	</div>

	<form class="master" method="post" id="vform2" action="" enctype="multipart/form-data" id="vform2">
		<h2>修改 {{chapter.title}}  <a href="#" class="remove" id="del_gallery">删除图集</a></h2>
		{% include "inc/form/upload-contents-gallery.html"  %}
            <div id="pChangeImageDesc" class="overlay control" style="display: none">
                    <label><span>图片标题</span> <input id="title" class="validate[required]" type="text" name="title" placeholder="请输入标题"/></label>
                    <label><span>选择图片</span> <input id="picture" name="picture" type="file"/></label>
                    <label><span>图片说明</span> <textarea id="content" name="content" rows="8" cols="30" placeholder="请输入说明"></textarea></label>
                    <input type="hidden" id="gallery_id" name="gallery_id" value="" work="{{work.id}}" gid="{{chapter.id}}"/>
                    <input type="hidden" id="section_id" name="section_id" value="0"/>
		    <div class="label">
                        <button type="submit">确认</button>
                        <!-- <button id="btnCommitPictureMeta">确认</button> -->
                        <button id="btnCancelPictureMeta">取消</button>
			<a href="#" class="remove" id="del_gallery_section">删除</a>
                </div>
            </div>
            <div class="label">
		    <button onclick="location.href='{{work.get_absolute_url}}'">确认</button>
	        </div>
        </form>
        
</div>

{% include "inc/footer.html" %}</body>
{% endblock %}
