from django.conf.urls.defaults import *

urlpatterns = patterns('synclee.tab.views',
    
    url(r'^(?P<slug_name>[\w-]+)/$', 'showTab', name='showTab'),
    url(r'^(?P<slug_name>[\w-]+)/filter/$', 'showObject', name='showObject'),
    #url(r'^', 'showTab', name='ShowAllTab'),
)