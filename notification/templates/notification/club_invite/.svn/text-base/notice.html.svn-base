{% extends 'base.html' %}
{% load i18n %}

{% block notice_content %}
<a href="{{from_user.get_absolute_url}}">{{ from_user.get_profile.get_name }}</a> 邀请您加入社团 <a href="{{club.get_absolute_url}}">{{club.name}}</a> <br>
理由：{{membership.reason}}<br>
<a href="{% url AcceptClubMember %}?membership_id={{membership.id}}">接受</a> <a href="{% url DenyClubMember %}?membership_id={{membership.id}}">拒绝</a>
{% endblock %}